<Application
    x:Class="McenterLite.Widget.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    RequestedTheme="Dark">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!--
                The Game Bar hosts widgets over live gameplay, so the palette is deliberately
                dark and low-contrast against its own chrome rather than a standard app surface.
            -->
            <SolidColorBrush x:Key="CardBackgroundBrush" Color="#1A1F26" />
            <SolidColorBrush x:Key="CardBorderBrush" Color="#2C3540" />
            <SolidColorBrush x:Key="AccentBrush" Color="#4DA3FF" />
            <SolidColorBrush x:Key="WarningBrush" Color="#E8A33D" />
            <SolidColorBrush x:Key="ErrorBrush" Color="#E8624D" />
            <SolidColorBrush x:Key="MutedTextBrush" Color="#8B97A6" />

            <Style x:Key="CardStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="Padding" Value="12" />
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>

            <Style x:Key="CardHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Margin" Value="0,0,0,8" />
            </Style>

            <Style x:Key="HintStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="11" />
                <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
                <Setter Property="TextWrapping" Value="Wrap" />
                <Setter Property="Margin" Value="0,4,0,0" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
